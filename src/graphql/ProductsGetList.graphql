query ProductsGetList($skip: Int!, $take: Int!) {
	products(take: $take, skip: $skip) {
		data {
			...ProductItem
			categories {
				...CategoryItem
			}
			collections {
				...CollectionItem
			}
		}
		meta {
			count
			total
		}
	}
}
