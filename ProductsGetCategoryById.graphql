query ProductsGetByCategoryById($id: ID!) {
	category(id: $id) {
		id
		name
		description
		products {
			id
			name
			slug
			description
			longDescription
			categoryId
			price
			rating
			image
		}
	}
}
